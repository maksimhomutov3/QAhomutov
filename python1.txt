# Используйте цикл for для вычисления суммы всех чётных чисел в диапазоне от 10 до 30 (включая крайние значения).
# Выведите результат на печать
num = 0
for element in range(10, 31):
    if element % 2 == 0:
        num += element
print(num)


# Получите от пользователя число на вводе и используйте цикл for для вывода на экран текста 'Hi' столько же раз
number = input("Напишите любое число")
for e in range(int(number)):
    print("Hi")

# Написал простой калькулятор массы тела
name = "Max"
height = 2
wight = 95

bmi = wight / (height**2)
print("Индекс массы тела равен : " + str(bmi))
if bmi < 25:
    print("У " + name +" нет лишнего веса")
else:
    print("У " + name + " есть лишний вес")
